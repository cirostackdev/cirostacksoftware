"use client";

// SEO is handled by Next.js generateMetadata in each app/*/page.tsx.
// This stub keeps existing page imports compiling without changes.

interface SEOProps {
    title?: string;
    description?: string;
    canonical?: string;
    type?: "website" | "article" | "profile";
    image?: string;
    url?: string;
}

export function SEO(_props: SEOProps) {
    return null;
}
